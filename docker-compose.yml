version: "3"
services:
  mysql:
    restart: always
    image: mysql:5.7
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 123qwe
    volumes:
      - "./.mysql-data/db:/var/lib/mysql"
